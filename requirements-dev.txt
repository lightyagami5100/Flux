# Development and CI dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
httpx==0.28.1            # used by fastapi.testclient
ruff==0.15.3             # lint gate referenced by .qoder/rules/Flux-sys.md
