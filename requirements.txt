# --- Runtime: API + worker -------------------------------------------------
# Pinned to the versions this project is verified against.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.32

# --- Persistence -----------------------------------------------------------
SQLAlchemy==2.0.52
greenlet==3.5.5          # required by SQLAlchemy's async engine
asyncpg==0.31.0          # PostgreSQL / PostGIS driver
aiosqlite==0.22.1        # SQLite driver used by the local dev fallback in main.py

# --- Queue / cache ---------------------------------------------------------
redis==8.1.0
fakeredis==2.37.1        # in-memory fallback when Redis is unreachable (main.py lifespan)

# --- Object storage --------------------------------------------------------
minio==7.2.20

# --- Frame decoding (no local model inference) ------------------------------
# headless build: no GUI/X11 deps, correct for containers and servers.
opencv-python-headless==5.0.0.93
numpy==2.5.2

# --- External CV inference (Roboflow HTTP API) ------------------------------
# inference-sdk has no wheels for Python >= 3.13; the container uses 3.11.
# Local dev on newer interpreters degrades gracefully (see app/processors/roboflow.py).
inference-sdk>=0.9.0; python_version < "3.13"

# --- Resilience ------------------------------------------------------------
tenacity==9.1.4
